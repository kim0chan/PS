#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0);
    cin.sync_with_stdio(0);
    
    int N;
    cin >> N;
    for(int i = 1; i <= N; i++) {
        cout << i << '\n';
    }
}
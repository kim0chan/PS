#include <bits/stdc++.h>
using namespace std;

int main(void) {
	cin.sync_with_stdio(0);
	cin.tie(0);

	string input;
	for (int i = 0; i < 100; i ++) {
		getline(cin, input);
		cout << input << '\n';
	}
}